Thanks for checking out my demo code.

To get going, simply follow these steps (assuming you are on a mac).

$ cd <project root>
$ virtualenv env
$ source env/bin/activate
$ pip install -r requirements.txt
$ python src/manage.py setupdb
$ python src/manage.py runserver 
$ open http://localhost:5000  
